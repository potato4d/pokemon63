// const { execSync } = require('child_process')

// for (let index = 1; index <= 530; index++) {
//   console.log(
//     `convert ./out/${index}.png -background "#f5f5f5" -alpha remove -alpha off ./out2/${index}.png`
//   )
//   execSync(
//     `convert ./out/${index}.png -background "#f5f5f5" -alpha remove -alpha off ./out2/${index}.png`
//   )
// }
